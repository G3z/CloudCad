define(
    "views/draw/3D/primitives/Primitive"
    ()->
        class CC.views.draw.primitives.Primitive
            @id
            @name
            constructor:()->
                @id = Math.guid()
            
            
)